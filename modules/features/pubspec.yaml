name: features
description: A new Flutter project.
version: 0.0.1

publish_to: 'none'

environment:
  sdk: ^3.5.4

dependencies:
  flutter:
    sdk: flutter
  dependencies:
    path: ../dependencies
  core:
    path: ../core
  art_core:
    path: ../art_core

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0
  mockito: ^5.0.15
  build_runner: ^2.4.5
  build_test: ^2.2.2
  bloc_test: ^9.1.7
